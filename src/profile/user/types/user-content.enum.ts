// Enum for user content visibility
export enum UserContent {
  COMPLETE = "COMPLETE", // whole info (User interface)
  PRIVATE = "PRIVATE", // private view (UserPrivateViewDto interface)
  PUBLIC = "PUBLIC" // public view (UserPublicViewDto interface)
}
